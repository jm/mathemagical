#!/usr/bin/env ruby

$:.unshift File.dirname(__FILE__) + '/../lib'

require 'rubygems'
require 'mathemagical'
require 'mathemagical/string'

inline = (ARGV[0] == "block")
STDOUT.puts STDIN.read.to_mathml(inline)